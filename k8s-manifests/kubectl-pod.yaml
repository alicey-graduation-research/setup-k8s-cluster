apiVersion: v1
kind: Pod
metadata:
  labels:
    app: kubectl
  name: kubectl-pod
spec:
  containers:
  - name: kubectl
    image: alicey/kubectl:latest
    command:
    - tail
    - -f
    - /dev/null